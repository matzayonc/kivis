[workspace]
members = ["fs-store", "kivis", "kivis-derive"]
resolver = "2"

[workspace.dependencies]
kivis = { path = "kivis" }
kivis-derive = { path = "kivis-derive" }

serde = { version = "1.0.219", features = ["derive"] }
bincode = { version = "2.0.1", features = ["serde"] }
hex = "0.4.3"
